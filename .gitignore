node_modules
yarn.lock

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Storybook build output
storybook-static

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# SQLite
*.db

# Claude instructions
CLAUDE.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Coverage directory used by testing tools
coverage
.nyc_output

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/


# Telegram bot token (secret — never commit)
.telegram-bot-token

coverage-e2e

# Local scratch that is not part of the app. These are large (video-frames alone is ~240 MB) and
# sit at the repo root, so a reflexive `git add -A` sweeps them into a commit. Ignoring them makes
# the safe command the easy one.
/Datamonkey_2_0/
/video-frames/
/benchmark-results/
/hyphy-wasm-artifact/
/src/benchmark/test-alignments/

# onnxruntime-web WASM runtime, copied from node_modules at build time by
# scripts/copy-ort-wasm.mjs. Pinned by package.json, not by git.
static/ort/

# Local working notes and agent definitions — not part of the app
.claude/
UX-QUALITY-OF-LIFE.md
