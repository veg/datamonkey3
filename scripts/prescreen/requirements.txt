# Verification-only dependencies for the MEME hit-likelihood model.
#
# These are NOT application dependencies and must never become one. DM3 ships no ONNX runtime to
# the browser — the point of treeEnsemble.js is that the 13.5 MB of WASM stays out of the bundle.
# These packages exist so CI can prove the ~37 KB coefficient table still matches the .onnx the
# ML/QC teams validated. Nothing here reaches a user.
onnx>=1.16
onnxruntime>=1.17
numpy>=1.26
